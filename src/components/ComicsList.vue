<template>
  <section id="comicsList">
        <div class="container">

            <div id="section-title">
                current-series
            </div>

            <div class="comic" v-for="(comic, index) in comics" :key="index">
                <img :src="comic.thumb" :alt="comic.series">
                <p>{{comic.series}}</p>
            </div>

            <button>load more</button>
        </div>
  </section>
</template>

<script>
import comicsData from '../assets/dc-comics.json'

export default {
    name: 'ComicsList',
    data() {
        return {
            comics: comicsData
        }
    }
}
</script>

<style lang="scss" scoped>
@import "../assets/style/variables.scss";

#comicsList {
    background: $darkGrey;

    .container {
        padding-top: 40px;
        padding-bottom: 15px;
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        position: relative;

        #section-title {
            padding: 5px 20px;
            font-size: 1.6rem;
            font-weight: bold;
            background-color: $primaryColor;
            color: #fff;
            text-transform: uppercase;
            position: absolute;
            left: 0;
            top: 0;
            transform: translateY(-50%);
        }

        .comic {
            width: calc(100% / 6);
            padding: 0px 10px;
            margin-bottom: 30px;

            &:hover {
                cursor: pointer;
            }

            &:hover p {
                color: $primaryColor;
            }

            img {
                width: 100%;
                height: 170px;
                object-fit: cover;
                object-position: top;
            }

            p {
                text-transform: uppercase;
                color: #fff;
                margin-top: 10px;
                font-weight: bold;
            }
        }

        button {
            text-transform: uppercase;
            width: 150px;
            border-radius: 0;
            border: 1px solid $primaryColor;
            padding: 7px 0px;
            background-color: $primaryColor;
            color: #fff;
            font-weight: bold;

            &:hover {
                cursor: pointer;
                background-color: transparent;
            }
        }
    }
}
</style>